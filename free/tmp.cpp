#include <bits/stdc++.h>
using namespace std;
int arr[10002];
vector<int> two;
int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);

  int n;
  cin >> n;

  for (int i = 0; i < n; i++)
    cin >> arr[i];

  for (int i = 0; i < n; i++)
  {
    for (int j = 0; j < n; j++)
    {
      if (j == i)
      {
        
      }
    }
  }
}