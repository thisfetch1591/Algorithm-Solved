#include <bits/stdc++.h>
using namespace std;
int k, n;
int arr[10002];

bool solve(long long mid)
{
  int tmp = 0;
  for (int i = 0; i < n; i++)
  {
    
  }
}
int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> k >> n;

  for (int i = 0; i < k; i++)
    cin >> arr[i];

  long long st = 0;
  long long en = __LONG_LONG_MAX__;

  while (st < en)
  {
    long long mid = (st + en) / 2;
  }
}