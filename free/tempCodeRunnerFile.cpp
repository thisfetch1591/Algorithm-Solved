    // int max = price[n - 1];
    // for (int i = n - 2; i >= 0; --i)
    // {
    //   if (price[i] > max)
    //     max = price[i];
    //   ans += max - price[i];
    // }
    // cout << ans << '\n';
    // price.clear();