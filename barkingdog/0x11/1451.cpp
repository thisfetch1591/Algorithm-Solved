#include <bits/stdc++.h>
using namespace std;

int ans;
int neorpo = 1;
int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);

  string exp;
  cin >> exp;

  for (auto c : exp)
  {
    if (c == '+' || c == '-')
    {
      ans +=
    }
  }
}