#include <bits/stdc++.h>
using namespace std;
int tc, n;
int main(void) {
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> tc;

  while (tc--) {
    map<string, string> mp;
    for (int i = 0; i < n; i++) {
      string k, v;
      cin >> k, v;

      mp.insert(k, v);
    }
  }
}