#include <bits/stdc++.h>
using namespace std;
int n, m, x1, y1, x2, y2;
string board[302];
int main(void) {
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> n >> m >> x1 >> y1 >> x2 >> y2;

  for (int i = 0; i < n; i++) {
    cin >> board[i];
  }
}