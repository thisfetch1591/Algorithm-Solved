#include <bits/stdc++.h>
using namespace std;
int n, m;
string board[52];
int main(void) {
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> n >> m;
  for (int i = 0; i < n; i++) {
    cin >> board[i];
  }
}